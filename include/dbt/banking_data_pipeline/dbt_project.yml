name: 'banking_data_pipeline'
version: '1.0.0'
config-version: 2

profile: 'banking_data_pipeline'

model-paths: ["models"]
macro-paths: ["macros"]

models:
  banking_data_pipeline:
    
    1_staging:
      +schema: bronze
      +materialized: ephemeral

    2_intermediate:
      +schema: silver
      +materialized: view

    3_marts:
      +schema: gold
      +materialized: view